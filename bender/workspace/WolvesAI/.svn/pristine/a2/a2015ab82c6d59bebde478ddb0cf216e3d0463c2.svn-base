/*
 * Geometry.cpp
 *
 *  Created on: 09.01.2014
 *      Author: Oliver Krebs
 */

/*
#include "Geometry.h"
#include <cmath>

// not used so commented out for LINT ...

double distancePointToLineSegment(const Point *point, const Point *linePointA, const Point *linePointB) {
	Point p2(linePointB->getX() - linePointA->getX(), linePointB->getY() - linePointA->getY());
	int val = p2.getX() * p2.getX() + p2.getY() * p2.getY();
	int valX = (point->getX() - linePointA->getX()) * p2.getX();
	int valY = (point->getY() - linePointA->getY()) * p2.getY();
	double u = 1.0;
	if( val != 0) {
		u = (double)(valX + valY) / (double)val;
	}

	if (u > 1.0) {
		u = 1.0;
	} else if (u < 0.0) {
		u = 0.0;
	}

	double x = (double)linePointA->getX() + u * p2.getX();
	double y = (double)linePointA->getY() + u * p2.getY();

	double dx = x - (double)point->getX();
	double dy = y - (double)point->getY();

	return sqrt(dx*dx + dy*dy);
}*/
